(*
Ocaml 에서의 함수 선언

Ocaml 에서의 함수 선언은 let 키워드를 사용한다.

let <함수이름> <매개변수> <매개변수> = <내용>

타입은 쓸 수도 있고, 쓰지 않을 수도 있으나 쓰는게 가독성 부분에서 더 좋다.

let <함수이름>:<타입> <매개변수> <매개변수> = <내용>

재귀함수는 rec 키워드를 사용하면 된다.

let rec <함수이름> <매개변수> = <내용>

Ocaml 에서의 return 문은 then 이다.

*)